{
  "name": "pollclass-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:codegen": "playwright codegen",
    "dev": "npm run dev --prefix ../Lab1/pollclass",
    "install:deps": "npm install --prefix ../Lab1/pollclass"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  }
}