<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# 2. Laboratorio: Servidor MCP local con GitHub Copilot en VS Code

Vence mañana a las 23:00
Instrucciones
Construye un servidor MCP local en Python, registralo en .vscode/mcp.json y ejecuta sus herramientas desde GitHub Copilot Chat en modo Agent dentro de Visual Studio Code. Duracion sugerida en clase: 60 minutos.
Objetivo: demostrar que Copilot (cliente) puede descubrir e invocar tools expuestas por server.py (servidor) a traves del protocolo MCP por stdio.
Requisitos:
VS Code con la carpeta qaLabMcp abierta (no solo el archivo).

- Python instalado en Windows (python --version) y la extension Python en VS Code.
- GitHub Copilot Chat con sesion iniciada.
- Instalar el SDK: python -m pip install "mcp[cli]".
Estructura final esperada: qaLabMcp/.vscode/mcp.json y qaLabMcp/server.py

Parte 1 - Servidor base (server.py): crea un FastMCP llamado "qaLabMcp" con estas 3 tools y compila con python -m py_compile server.py:
validar_cliente(cip, telefono, email) - valida y normaliza datos de un cliente.
2. generar_caso_prueba(endpoint, metodo, escenario) - genera un caso de prueba funcional.
3. calcular_percentil_simple(valores, percentil) - calcula un percentil simple.
Parte 2 - Registro y conexion: crea .vscode/mcp.json (type stdio, command python, args server.py) y arranca con Ctrl+Shift+P > MCP: List Servers > qaLabMcp > Start Server. Regla: no ejecutes "python server.py" en otra terminal; VS Code maneja el proceso stdio.

Parte 3 - Pruebas desde Copilot (modo Agent): confirma que las tools aparecen y ejecuta los 3 prompts:
validar_cliente con CIP 12345, telefono 6677-8899, correo [prueba@demo.com](mailto:prueba@demo.com).
B) generar_caso_prueba para POST /api/login con credenciales invalidas.
C) calcular_percentil_simple del percentil 95 de [120,130,150,300,90,100,500,220].
Parte 4 - Retos practicos (agrega cada tool antes de if __name__ == "__main__" y reinicia el servidor):
Reto 1 (Basico): clasificar_error_http(status_code) -> Exito / Redireccion / Error del cliente / Error del servidor. Prueba: 500 -> Error del servidor.

- Reto 2 (Intermedio): evaluar_sla(p95_ms, limite_ms) -> cumple y diferencia en ms. Prueba: p95=480, limite=500 -> cumple.
- Reto 3 (Intermedio): validar_respuesta_api(status_code, tiempo_ms, limite_ms, tiene_token) -> valido solo si 2xx, el tiempo cumple y existe token. Prueba: 200,350,500,true -> valido.
- Reto 4 (Avanzado): datos_prueba.json + buscar_cliente(cip) que lea el archivo y devuelva el cliente o un mensaje de no encontrado.
Entregables: repositorio GitHub con server.py, .vscode/mcp.json y datos_prueba.json; y evidencia de cada prueba y reto (captura de la tool visible en Copilot, el prompt usado y el resultado devuelto).

Referencias oficiales: VS Code MCP Servers, GitHub Copilot MCP, MCP Python SDK.

ayudame con esto. desarrolla un plan de implementacion

Aquí tienes el plan de implementación completo y el código listo para copiar y pegar:

***

# Plan de Implementación — Servidor MCP Local con GitHub Copilot

## Estructura final del proyecto

```
qaLabMcp/
├── .vscode/
│   └── mcp.json
├── server.py
└── datos_prueba.json
```


***

## FASE 1 — Preparación del ambiente (5 min)

**Pasos:**

1. Crea la carpeta `qaLabMcp` en tu PC y ábrela en VS Code (`File > Open Folder`)
2. Verifica Python: abre terminal en VS Code y ejecuta:
```bash
python --version
```

3. Instala el SDK de MCP:
```bash
python -m pip install "mcp[cli]"
```

4. Verifica que tienes GitHub Copilot Chat activo con sesión iniciada.

***

## FASE 2 — Crear `server.py` (15 min)

Crea el archivo `qaLabMcp/server.py` con este contenido completo (incluye las 3 tools base + los 4 retos):

```python
import json
import re
import math
import os
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("qaLabMcp")


# ─────────────────────────────────────────────
# TOOLS BASE
# ─────────────────────────────────────────────

@mcp.tool()
def validar_cliente(cip: str, telefono: str, email: str) -> dict:
    """Valida y normaliza los datos de un cliente (CIP, teléfono y correo)."""
    errores = []

    # Validar CIP: solo dígitos, entre 4 y 10 caracteres
    cip_limpio = cip.strip()
    if not re.fullmatch(r"\d{4,10}", cip_limpio):
        errores.append("CIP inválido: debe contener entre 4 y 10 dígitos numéricos.")

    # Validar teléfono panameño: formato XXXX-XXXX
    tel_limpio = telefono.strip()
    if not re.fullmatch(r"\d{4}-\d{4}", tel_limpio):
        errores.append("Teléfono inválido: debe tener el formato XXXX-XXXX (ej. 6677-8899).")

    # Validar email básico
    email_limpio = email.strip().lower()
    if not re.fullmatch(r"[^@\s]+@[^@\s]+\.[^@\s]+", email_limpio):
        errores.append("Correo electrónico inválido.")

    if errores:
        return {"valido": False, "errores": errores}

    return {
        "valido": True,
        "datos_normalizados": {
            "cip": cip_limpio,
            "telefono": tel_limpio,
            "email": email_limpio,
        },
        "mensaje": "Cliente validado correctamente.",
    }


@mcp.tool()
def generar_caso_prueba(endpoint: str, metodo: str, escenario: str) -> dict:
    """Genera un caso de prueba funcional para un endpoint de API REST."""
    metodo_upper = metodo.strip().upper()
    metodos_validos = ["GET", "POST", "PUT", "PATCH", "DELETE"]
    if metodo_upper not in metodos_validos:
        return {"error": f"Método HTTP no reconocido: {metodo}. Use uno de {metodos_validos}"}

    caso = {
        "id_caso": f"TC_{metodo_upper}_{endpoint.replace('/', '_').strip('_')}",
        "endpoint": endpoint,
        "metodo": metodo_upper,
        "escenario": escenario,
        "precondiciones": [
            "El servidor API debe estar corriendo.",
            "La base de datos de prueba debe estar inicializada.",
        ],
        "datos_entrada": {},
        "pasos": [
            f"1. Enviar petición {metodo_upper} a {endpoint}",
            f"2. Pasar el escenario: '{escenario}'",
            "3. Capturar código de respuesta HTTP.",
            "4. Capturar cuerpo de respuesta JSON.",
        ],
        "resultado_esperado": {},
        "tipo": "funcional",
    }

    escenario_lower = escenario.lower()

    if "invalido" in escenario_lower or "incorrecto" in escenario_lower or "erroneo" in escenario_lower:
        caso["datos_entrada"] = {"username": "usuario_invalido", "password": "clave_erronea"}
        caso["resultado_esperado"] = {
            "status_code": 401,
            "mensaje": "Credenciales inválidas",
            "token": None,
        }
    elif "exitoso" in escenario_lower or "valido" in escenario_lower or "correcto" in escenario_lower:
        caso["datos_entrada"] = {"username": "usuario_test", "password": "clave_correcta"}
        caso["resultado_esperado"] = {
            "status_code": 200,
            "mensaje": "Login exitoso",
            "token": "<jwt_token>",
        }
    elif "vacio" in escenario_lower or "sin datos" in escenario_lower:
        caso["datos_entrada"] = {}
        caso["resultado_esperado"] = {
            "status_code": 400,
            "mensaje": "Campos requeridos faltantes",
        }
    else:
        caso["datos_entrada"] = {"parametro": "valor_de_prueba"}
        caso["resultado_esperado"] = {
            "status_code": 200,
            "mensaje": "Respuesta esperada según el escenario",
        }

    return caso


@mcp.tool()
def calcular_percentil_simple(valores: list[float], percentil: float) -> dict:
    """Calcula un percentil simple de una lista de valores numéricos."""
    if not valores:
        return {"error": "La lista de valores no puede estar vacía."}
    if not (0 <= percentil <= 100):
        return {"error": "El percentil debe estar entre 0 y 100."}

    ordenados = sorted(valores)
    n = len(ordenados)
    indice = (percentil / 100) * (n - 1)
    inferior = int(math.floor(indice))
    superior = int(math.ceil(indice))

    if inferior == superior:
        resultado = ordenados[inferior]
    else:
        fraccion = indice - inferior
        resultado = ordenados[inferior] + fraccion * (ordenados[superior] - ordenados[inferior])

    return {
        "percentil": percentil,
        "resultado": round(resultado, 2),
        "valores_ordenados": ordenados,
        "total_valores": n,
        "interpretacion": f"El {percentil}% de los valores es menor o igual a {round(resultado, 2)}",
    }


# ─────────────────────────────────────────────
# RETO 1 (Básico): clasificar_error_http
# ─────────────────────────────────────────────

@mcp.tool()
def clasificar_error_http(status_code: int) -> dict:
    """Clasifica un código de estado HTTP en su categoría correspondiente."""
    if 200 <= status_code <= 299:
        categoria = "Éxito"
        descripcion = "La solicitud fue procesada correctamente."
    elif 300 <= status_code <= 399:
        categoria = "Redirección"
        descripcion = "Se requiere una acción adicional para completar la solicitud."
    elif 400 <= status_code <= 499:
        categoria = "Error del cliente"
        descripcion = "La solicitud contiene errores del lado del cliente."
    elif 500 <= status_code <= 599:
        categoria = "Error del servidor"
        descripcion = "El servidor falló al procesar una solicitud válida."
    else:
        categoria = "Desconocido"
        descripcion = "Código de estado HTTP no estándar."

    return {
        "status_code": status_code,
        "categoria": categoria,
        "descripcion": descripcion,
    }


# ─────────────────────────────────────────────
# RETO 2 (Intermedio): evaluar_sla
# ─────────────────────────────────────────────

@mcp.tool()
def evaluar_sla(p95_ms: float, limite_ms: float) -> dict:
    """Evalúa si el percentil 95 de latencia cumple con el SLA definido."""
    cumple = p95_ms <= limite_ms
    diferencia = round(limite_ms - p95_ms, 2)

    return {
        "p95_ms": p95_ms,
        "limite_ms": limite_ms,
        "cumple_sla": cumple,
        "diferencia_ms": diferencia,
        "mensaje": (
            f"✅ CUMPLE el SLA. Margen disponible: {diferencia} ms."
            if cumple
            else f"❌ NO CUMPLE el SLA. Excede en {abs(diferencia)} ms."
        ),
    }


# ─────────────────────────────────────────────
# RETO 3 (Intermedio): validar_respuesta_api
# ─────────────────────────────────────────────

@mcp.tool()
def validar_respuesta_api(
    status_code: int,
    tiempo_ms: float,
    limite_ms: float,
    tiene_token: bool,
) -> dict:
    """
    Valida una respuesta de API. Es válida solo si:
    - El status_code es 2xx
    - El tiempo_ms no supera el limite_ms
    - tiene_token es True
    """
    es_2xx = 200 <= status_code <= 299
    cumple_tiempo = tiempo_ms <= limite_ms
    checks = {
        "status_2xx": {"resultado": es_2xx, "detalle": f"Status {status_code} {'es' if es_2xx else 'no es'} 2xx"},
        "tiempo_dentro_limite": {"resultado": cumple_tiempo, "detalle": f"{tiempo_ms}ms {'≤' if cumple_tiempo else '>'} {limite_ms}ms"},
        "token_presente": {"resultado": tiene_token, "detalle": "Token presente" if tiene_token else "Token ausente"},
    }

    es_valida = es_2xx and cumple_tiempo and tiene_token

    return {
        "valida": es_valida,
        "veredicto": "✅ VÁLIDA" if es_valida else "❌ INVÁLIDA",
        "checks": checks,
        "resumen": f"Status={status_code}, Tiempo={tiempo_ms}ms (límite {limite_ms}ms), Token={'sí' if tiene_token else 'no'}",
    }


# ─────────────────────────────────────────────
# RETO 4 (Avanzado): buscar_cliente desde JSON
# ─────────────────────────────────────────────

@mcp.tool()
def buscar_cliente(cip: str) -> dict:
    """Busca un cliente por CIP en el archivo datos_prueba.json."""
    datos_path = os.path.join(os.path.dirname(__file__), "datos_prueba.json")

    if not os.path.exists(datos_path):
        return {"error": "El archivo datos_prueba.json no fue encontrado."}

    try:
        with open(datos_path, "r", encoding="utf-8") as f:
            datos = json.load(f)
    except json.JSONDecodeError as e:
        return {"error": f"Error al leer datos_prueba.json: {str(e)}"}

    clientes = datos.get("clientes", [])
    for cliente in clientes:
        if str(cliente.get("cip", "")).strip() == str(cip).strip():
            return {"encontrado": True, "cliente": cliente}

    return {
        "encontrado": False,
        "mensaje": f"No se encontró ningún cliente con CIP '{cip}'.",
    }


# ─────────────────────────────────────────────
if __name__ == "__main__":
    mcp.run(transport="stdio")
```


***

## FASE 3 — Crear `datos_prueba.json` (2 min)

Crea `qaLabMcp/datos_prueba.json`:

```json
{
  "clientes": [
    {
      "cip": "12345",
      "nombre": "Juan Pérez",
      "telefono": "6677-8899",
      "email": "juan.perez@demo.com",
      "estado": "activo"
    },
    {
      "cip": "67890",
      "nombre": "María González",
      "telefono": "6123-4567",
      "email": "maria.gonzalez@demo.com",
      "estado": "activo"
    },
    {
      "cip": "11111",
      "nombre": "Carlos Rodríguez",
      "telefono": "6999-0001",
      "email": "carlos.rodriguez@demo.com",
      "estado": "inactivo"
    },
    {
      "cip": "22222",
      "nombre": "Ana López",
      "telefono": "6543-2109",
      "email": "ana.lopez@demo.com",
      "estado": "activo"
    }
  ]
}
```


***

## FASE 4 — Crear `.vscode/mcp.json` (2 min)

Crea la carpeta `.vscode` dentro de `qaLabMcp` y el archivo `mcp.json`:

```json
{
  "servers": {
    "qaLabMcp": {
      "type": "stdio",
      "command": "python",
      "args": ["${workspaceFolder}/server.py"]
    }
  }
}
```


***

## FASE 5 — Verificar compilación (1 min)

En la terminal de VS Code dentro de la carpeta `qaLabMcp`:

```bash
python -m py_compile server.py
```

Si no imprime nada → todo está bien. Si hay error, revisar la línea indicada.

***

## FASE 6 — Arrancar el servidor en VS Code (3 min)

1. Presiona `Ctrl+Shift+P`
2. Escribe **MCP: List Servers** y selecciónalo
3. Verás `qaLabMcp` en la lista
4. Haz clic en **Start Server**
5. El servidor arranca por **stdio** — VS Code maneja el proceso, no abras otra terminal

***

## FASE 7 — Pruebas desde Copilot Chat (modo Agent)

Abre **Copilot Chat** (`Ctrl+Alt+I`), cambia a modo **Agent** y usa estos prompts exactos:

### Prueba A — validar_cliente

```
Usa la tool validar_cliente con CIP 12345, teléfono 6677-8899 y correo prueba@demo.com
```


### Prueba B — generar_caso_prueba

```
Usa la tool generar_caso_prueba para el endpoint POST /api/login con el escenario credenciales inválidas
```


### Prueba C — calcular_percentil_simple

```
Usa la tool calcular_percentil_simple para el percentil 95 de los valores [120, 130, 150, 300, 90, 100, 500, 220]
```


### Reto 1

```
Usa la tool clasificar_error_http con status_code 500
```


### Reto 2

```
Usa la tool evaluar_sla con p95_ms 480 y limite_ms 500
```


### Reto 3

```
Usa la tool validar_respuesta_api con status_code 200, tiempo_ms 350, limite_ms 500 y tiene_token true
```


### Reto 4

```
Usa la tool buscar_cliente con CIP 12345
```


***

## Checklist final para el repositorio GitHub

| Archivo | ✅ |
| :-- | :-- |
| `qaLabMcp/server.py` | 7 tools implementadas |
| `qaLabMcp/.vscode/mcp.json` | type stdio, command python |
| `qaLabMcp/datos_prueba.json` | 4 clientes de prueba |
| Capturas de pantalla de cada prueba en Copilot | Evidencia requerida |


***

## Tips importantes

- **Si el servidor no aparece:** cierra y vuelve a abrir la carpeta con `File > Open Folder` (debe ser la carpeta `qaLabMcp`, no el archivo)
- **Si Copilot no muestra las tools:** reinicia el servidor desde `MCP: List Servers` → Stop → Start
- **Si hay error de módulo:** asegúrate de que el pip install se hizo en el mismo Python que VS Code usa (`python -m pip install "mcp[cli]"`)
- **Para las capturas:** asegúrate de que se vea el nombre de la tool invocada, el prompt que usaste y el resultado JSON devuelto

